# Pipeline CI/CD - Ambiente Test
# Trigger: workflow_dispatch o tag test-*
# Ver plan.md FASE 5 para detalle completo
name: Pipeline Test

on:
  workflow_dispatch:
  push:
    tags: ["test-*"]

jobs:
  # TODO: implementar en FASE 5
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Pipeline Test - pendiente implementación FASE 5"
