# Pipeline CI/CD - Ambiente Dev
# Trigger: push a main
# Ver plan.md FASE 5 para detalle completo
name: Pipeline Dev

on:
  push:
    branches: [main]

jobs:
  # TODO: implementar en FASE 5
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Pipeline Dev - pendiente implementación FASE 5"
